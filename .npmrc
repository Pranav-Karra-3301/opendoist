# OpenDoist is an open-source project: always resolve from the public npm registry
# (overrides any machine-level private-mirror configuration so pnpm-lock.yaml is portable and CI-installable)
registry=https://registry.npmjs.org/
