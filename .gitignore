# OS
.DS_Store

# deps / builds (scaffold will extend)
node_modules/
dist/

# local data
/data/
*.local
.env

# playwright artifacts (e2e/.auth holds a real session — never commit)
apps/web/e2e/.auth/
apps/web/test-results/
apps/web/playwright-report/
